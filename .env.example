APP_ENV=development
API_URL=http://localhost:4000

# 32 bytes or less. Used for low-risk encrypted app preferences in MMKV.
# Do not store backend private keys or high-value secrets in client env files.
MMKV_ENCRYPTION_KEY=bare-mmkv-key-32-byte-fallback!!
